declare module 'react-router-dom' {
    export {
      BrowserRouter,
      HashRouter,
      Link,
      NavLink,
      Route,
      Switch,
      Redirect,
      useHistory,
      useLocation,
      useParams,
      useRouteMatch,
      withRouter,
      MemoryRouter,
      Prompt,
      generatePath,
      matchPath,
      Router,
      Routes,
      RouterProps,
      match,
      RouteChildrenProps,
      RouteComponentProps,
      RouteProps,
      StaticContext,
    } from 'react-router';
  
    export { __RouterContext as RouterContext } from 'react-router';
  
    export * from 'react-router';
  
  }
  