import './Catalog.css'

import {SquareLiner} from './SquareLiner'

export const Catalog = () => {
    return (
        <div className = "Container">
            <div className= "Catalog">
                <p>OII</p>
            </div>
            <SquareLiner />
        </div>
    )
}